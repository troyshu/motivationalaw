<%= form_for(@wallpaper) do |f| %>
  <% if @wallpaper.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wallpaper.errors.count, "error") %> prohibited this wallpaper from being saved:</h2>

      <ul>
      <% @wallpaper.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :quote_id %><br />
    <%= f.text_field :quote_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
